####### .md = markdown

# Git config

## configuration settings
 
Global settings for new git configuration 
only need to set once on a workstation 


`git_config --global user.name "my name"`

`git_config --global user.email "my edu email"`

